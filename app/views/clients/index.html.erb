<div class="page-header clearfix">
  <div class="pull-left">
    <h1>Lista de Clientes<small></small></h1>
  </div>
  <div class="pull-right">
    <div class="btn-group">
      <%= link_to 'Novo Cliente', new_client_path, class: "btn btn-primary" %>
    </div>
  </div>
</div>

<div class="table-responsive">
<table class="table table-striped">
  <thead>
    <tr>
      <th class="col-md-3">Nome</th>
      <th>Telefone</th>
      <th>Email</th>
      <!--<th>Endereço</th>-->
      <th>Ativo</th>
      <!--<th class="col-md-1"></th>-->
      <th class="col-md-1"></th>
      <th class="col-md-1"></th>
    </tr>
  </thead>

  <tbody>
    <% @clients.each do |client| %>
      <tr>
        <td><%= link_to client.full_name, client %></td>
        <td><%= client.phone %></td>
        <td><%= client.email %></td>
        <!--<td><%= client.address %></td>-->
        <td><%= client.active %></td>
        <!--<td><%= link_to 'Exibir', client %></td>-->
        <td><%= link_to edit_client_path(client) do %><span class="glyphicon glyphicon-edit" aria-hidden="true"></span><% end %></td>
        <td><%= link_to client, method: :delete, data: { confirm: 'Tem certeza?' } do %><span class="glyphicon glyphicon-trash" aria-hidden="true"></span><% end %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
<br>
